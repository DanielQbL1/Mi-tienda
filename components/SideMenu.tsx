import React from 'react';
import Icon from './Icon';
import { User, StoreSettings } from '../types';
interface SideMenuProps { isOpen: boolean; onClose: () => void; user: User | null; onLoginClick: () => void; onNavigate: (view: any) => void; settings: StoreSettings; }
const SideMenu: React.FC<SideMenuProps> = ({ isOpen, onClose, user, onLoginClick, onNavigate, settings }) => {
  if (!isOpen) return null;
  return ( <div className="fixed inset-0 z-[100] z-50"> <div className="absolute inset-0 bg-black/50 backdrop-blur-sm animate-fade-in" onClick={onClose} /> <div className="absolute top-0 bottom-0 left-0 w-[80%] max-w-[300px] bg-white shadow-2xl animate-slide-right flex flex-col"> <div className="p-6 border-b border-gray-100 flex items-center justify-between bg-gray-50"><div className="flex items-center gap-3">{user ? (<img src={user.avatar} className="w-12 h-12 rounded-full border-2 border-white shadow-sm" />) : (<div className="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center text-gray-400"><Icon name="User" size={24} /></div>)}<div><h3 className="font-bold text-gray-900 leading-tight">{user ? user.name : 'Invitado'}</h3><p className="text-xs text-gray-500">{user ? user.email : 'Bienvenido'}</p></div></div></div> <div className="flex-1 overflow-y-auto p-4 space-y-2"> {!user && (<button onClick={() => { onClose(); onLoginClick(); }} className="w-full flex items-center gap-4 p-4 rounded-xl bg-black text-white shadow-lg shadow-black/20 mb-6 hover:bg-gray-900 transition-colors" style={{ backgroundColor: settings.primaryColor }}><Icon name="LogOut" size={20} className="rotate-180" /><span className="font-bold text-sm">Iniciar Sesión / Registro</span></button>)} <div className="space-y-1"><button onClick={() => { onClose(); onNavigate('HOME'); }} className="w-full flex items-center gap-4 p-3 rounded-xl hover:bg-gray-50 text-gray-700 transition-colors"><Icon name="Home" size={20} /><span className="font-medium text-sm">Inicio</span></button></div> </div> <div className="p-6 border-t border-gray-100 text-center"><p className="text-xs text-gray-400 font-medium">© 2024 {settings.name}</p></div> </div> </div> );
};
export default SideMenu;